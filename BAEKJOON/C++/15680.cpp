#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

using namespace std;

int main(void) {
	int count;
	scanf("%d", &count);
	count == 1 ? printf("Leading the Way to the Future") : printf("YONSEI");
	return 0;

}